<%- include(`./indexLayout/${locals.setting.indexLayout}`) %>
    <script>
        window.addEventListener("flutterInAppWebViewPlatformReady", function (event) {
            window.flutter_inappwebview.callHandler('UserInfo').then(function (result) {
                //console.log(result);
                //alert(JSON.stringify(result));
                const ids = JSON.stringify(result);
                //alert(result.userId);
                window.localStorage.setItem("mobileId", result.userId);
                //const ids2 = window.localStorage.getItem("userId");
                //alert(ids2);
                const mobileId = {
                    "mobileId": result.userId,
                }
                $.ajax({
                    type: "post",
                    url: "https://vetween.kr/mobileIdsend",
                    headers: { 'Content-type': 'application/json' },
                    data: JSON.stringify(mobileId),
                })
            });
        });


    </script>